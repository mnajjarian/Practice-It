/*The following code is poorly structured. It contains redundant statements that unnecessarily occur multiple times. Factor out 
redundant code from the following example by moving it out of the if/else statement, while preserving the same behavior and output.*/

if (x < 30) {
    x++;
}
    a = 2;
    System.out.println("Java is awesome! " + x);
